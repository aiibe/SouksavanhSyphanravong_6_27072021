{
  "name": "fisheye",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "node ./staticBuild.js",
    "sass": "npx sass --watch src/scss/main.scss:dist/css/style.sass.css --no-source-map --style=compressed",
    "prefix": "npx postcss --watch dist/css/style.sass.css -o dist/css/style.css --no-map --use autoprefixer",
    "style": "npm-run-all --parallel sass prefix"
  },
  "browserslist": [
    "last 4 version"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "handlebars": "^4.7.7",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.3.6",
    "postcss-cli": "^8.3.1",
    "sass": "^1.36.0"
  }
}